services:
  karton-port_scanner:
    environment:
        SCANNING_PACKETS_PER_SECOND_PER_IP: 30

  karton-bruter:
    environment:
      SECONDS_PER_REQUEST_FOR_ONE_IP: 0
  redis:
    volumes:
      - data-test-redis:/data

  db:
    volumes:
     - data-test-mongodb:/data/db

volumes:
  data-test-mongodb:
  data-test-redis:
