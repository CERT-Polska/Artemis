apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: artemis-dns-scanner
  name: artemis-dns-scanner
  namespace: artemis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-dns-scanner
  template:
    metadata:
      labels:
        app: artemis-dns-scanner
    spec:
      containers:
      - command:
        - python3
        - -m
        - artemis.modules.dns_scanner
        env:
        - name: DB_CONN_STR
          valueFrom:
            secretKeyRef:
              key: db_conn_str
              name: artemis
        image: certpl/artemis:latest
        imagePullPolicy: Always
        name: artemis-dns-scanner
        volumeMounts:
        - mountPath: /etc/karton
          name: karton-cfg
      imagePullSecrets:
      - name: ghcr
      restartPolicy: Always
      volumes:
      - name: karton-cfg
        secret:
          defaultMode: 420
          secretName: karton
