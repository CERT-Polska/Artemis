{% if "exposed_log_file" in data.contains_type %}
    <li>{% trans %}The following addresses contain logs:{% endtrans %}
        <ul>
            {% for report in data.reports %}
                {% if report.report_type == "exposed_log_file" %}
                    <li>
                        {{ report.target }}
                        {{ report_meta(report) }}
                    </li>
                {% endif %}
            {% endfor %}
        </ul>
        <p>
            {% trans trimmed %}
                Such files shouldn't be publicly accessible as they may contain personal identifiable
                information, information that allows unauthorized access (such as session identifiers),
                configuration data or other types of information that shouldn't be publicly accessible.
            {% endtrans %}
        </p>
    </li>
{% endif %}
