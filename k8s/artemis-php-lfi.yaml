apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: artemis-php-lfi
  name: artemis-php-lfi
  namespace: artemis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-php-lfi
  template:
    metadata:
      labels:
        app: artemis-php-lfi
    spec:
      containers:
      - command:
        - python3
        - -m
        - artemis.modules.php_lfi
        env:
        - name: DB_CONN_STR
          valueFrom:
            secretKeyRef:
              key: db_conn_str
              name: artemis
        image: certpl/artemis:latest
        imagePullPolicy: Always
        name: artemis-bruter
        volumeMounts:
        - mountPath: /etc/karton
          name: karton-cfg
      imagePullSecrets:
      - name: ghcr
      restartPolicy: Always
      volumes:
      - name: karton-cfg
        secret:
          defaultMode: 420
          secretName: karton
