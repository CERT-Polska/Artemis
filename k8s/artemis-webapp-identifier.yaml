apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: artemis-webapp-identifier
  name: artemis-webapp-identifier
  namespace: artemis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-webapp-identifier
  template:
    metadata:
      labels:
        app: artemis-webapp-identifier
    spec:
      containers:
      - command:
        - python3
        - -m
        - artemis.modules.webapp_identifier
        env:
        - name: DB_CONN_STR
          valueFrom:
            secretKeyRef:
              key: db_conn_str
              name: artemis
        image: certpl/artemis:latest
        imagePullPolicy: Always
        name: artemis-webapp-identifier
        volumeMounts:
        - mountPath: /etc/karton
          name: karton-cfg
      imagePullSecrets:
      - name: ghcr
      restartPolicy: Always
      volumes:
      - name: karton-cfg
        secret:
          defaultMode: 420
          secretName: karton
