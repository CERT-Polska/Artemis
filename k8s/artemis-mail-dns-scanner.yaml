apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: artemis-mail-dns-scanner
  name: artemis-mail-dns-scanner
  namespace: artemis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-mail-dns-scanner
  template:
    metadata:
      labels:
        app: artemis-mail-dns-scanner
    spec:
      containers:
      - command:
        - python3
        - -m
        - artemis.modules.mail_dns_scanner
        env:
        - name: DB_CONN_STR
          valueFrom:
            secretKeyRef:
              key: db_conn_str
              name: artemis
        image: certpl/artemis:latest
        imagePullPolicy: Always
        name: artemis-mail-dns-scanner
        volumeMounts:
        - mountPath: /etc/karton
          name: karton-cfg
      imagePullSecrets:
      - name: ghcr
      restartPolicy: Always
      volumes:
      - name: karton-cfg
        secret:
          defaultMode: 420
          secretName: karton
