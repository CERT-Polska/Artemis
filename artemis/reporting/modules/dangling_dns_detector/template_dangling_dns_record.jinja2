{% if "dangling_dns_record" in data.contains_type %}
    <li>{% trans %}We identified a risk of subdomain takeover for the following subdomains:{% endtrans %}
        <ul>
            {% for report in data.reports %}
                {% if report.report_type == "dangling_dns_record" and loop.index0 < 5%}
                    <li>
                        {{ report.target }}: {{ _(report.additional_data.message_en) }} ({% trans %}the records points to{% endtrans %} {{ report.additional_data.target }})
                        {{ report_meta(report) }}
                    </li>
                {% endif %}
            {% endfor %}
            {% if data.reports | length > 5%}
                <p>
                {% trans %}Reporting is currently limited to first five results, rest was skipped.{% endtrans %}
                </p>
            {% endif %}
        </ul>
        <p>
            {% trans trimmed %}
                Please verify the configuration, and, if subdomain takeover is possible, change it. Subdomain
                takeover allows an attacker to put any content on the website, including malware. If the configuration
                is correct, please contact us in order to stop receiving subdomain takeover vulnerability on correctly
                configured domains.
            {% endtrans %}
        </p>
    </li>
{% endif %}
