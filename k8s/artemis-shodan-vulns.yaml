apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: artemis-shodan-vulns
  name: artemis-shodan-vulns
  namespace: artemis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-shodan-vulns
  template:
    metadata:
      labels:
        app: artemis-shodan-vulns
    spec:
      containers:
      - command:
        - python3
        - -m
        - artemis.modules.shodan_vulns
        env:
        - name: DB_CONN_STR
          valueFrom:
            secretKeyRef:
              key: db_conn_str
              name: artemis
        - name: SHODAN_API_KEY
          valueFrom:
            secretKeyRef:
              key: shodan_api_key
              name: artemis
        image: certpl/artemis:latest
        imagePullPolicy: Always
        name: artemis-shodan-vulns
        volumeMounts:
        - mountPath: /etc/karton
          name: karton-cfg
      imagePullSecrets:
      - name: ghcr
      restartPolicy: Always
      volumes:
      - name: karton-cfg
        secret:
          defaultMode: 420
          secretName: karton
