apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: artemis-wp-scanner
  name: artemis-wp-scanner
  namespace: artemis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-wp-scanner
  template:
    metadata:
      labels:
        app: artemis-wp-scanner
    spec:
      containers:
      - command:
        - python3
        - -m
        - artemis.modules.wp_scanner
        env:
        - name: DB_CONN_STR
          valueFrom:
            secretKeyRef:
              key: db_conn_str
              name: artemis
        image: certpl/artemis:latest
        imagePullPolicy: Always
        name: artemis-wp-scanner
        volumeMounts:
        - mountPath: /etc/karton
          name: karton-cfg
      imagePullSecrets:
      - name: ghcr
      restartPolicy: Always
      volumes:
      - name: karton-cfg
        secret:
          defaultMode: 420
          secretName: karton
