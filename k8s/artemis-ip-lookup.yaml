apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: artemis-ip-lookup
  name: artemis-ip-lookup
  namespace: artemis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-ip-lookup
  template:
    metadata:
      labels:
        app: artemis-ip-lookup
    spec:
      containers:
      - command:
        - python3
        - -m
        - artemis.modules.ip_lookup
        env:
        - name: DB_CONN_STR
          valueFrom:
            secretKeyRef:
              key: db_conn_str
              name: artemis
        image: certpl/artemis:latest
        imagePullPolicy: Always
        name: artemis-ip-lookup
        volumeMounts:
        - mountPath: /etc/karton
          name: karton-cfg
      imagePullSecrets:
      - name: ghcr
      restartPolicy: Always
      volumes:
      - name: karton-cfg
        secret:
          defaultMode: 420
          secretName: karton
