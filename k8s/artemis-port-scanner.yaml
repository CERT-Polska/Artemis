apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: artemis-port-scanner
  name: artemis-port-scanner
  namespace: artemis
spec:
  replicas: 5
  selector:
    matchLabels:
      app: artemis-port-scanner
  template:
    metadata:
      labels:
        app: artemis-port-scanner
    spec:
      containers:
      - command:
        - python3
        - -m
        - artemis.modules.port_scanner
        env:
        - name: DB_CONN_STR
          valueFrom:
            secretKeyRef:
              key: db_conn_str
              name: artemis
        image: certpl/artemis:latest
        imagePullPolicy: Always
        name: artemis-port-scanner
        volumeMounts:
        - mountPath: /etc/karton
          name: karton-cfg
      imagePullSecrets:
      - name: ghcr
      restartPolicy: Always
      volumes:
      - name: karton-cfg
        secret:
          defaultMode: 420
          secretName: karton
